<UserControl x:Class="DiplomaStorage.UserControls.MagicTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DiplomaStorage.UserControls"
             mc:Ignorable="d">
    <TreeView ItemsSource="{Binding DataContext.Group}">
        <TreeView.ItemTemplate>
            <HierarchicalDataTemplate>
                <Label Content="{Binding date, StringFormat=YY}" />
            </HierarchicalDataTemplate>
        </TreeView.ItemTemplate>
    </TreeView>
</UserControl>